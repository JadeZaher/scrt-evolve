//! Full-finetune preset — update all weights. ARCHIVED (track 06,
//! `conductor/tracks/_archived/`): the product trains LoRA adapters over an
//! immutable base, so `train::run` bails on this preset. Kept as a trait seam.
